import React from "react";
import { View, Text } from "react-native";

const Meetings = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="font-mada text-lg">No meetings scheduled</Text>
    </View>
  );
};

export default Meetings;
